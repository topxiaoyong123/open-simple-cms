<?xml version="1.0" encoding="UTF-8"?>
<beans xmlns="http://www.springframework.org/schema/beans"
       xmlns:context="http://www.springframework.org/schema/context"
       xmlns:xsi="http://www.w3.org/2001/XMLSchema-instance" xmlns:aop="http://www.springframework.org/schema/aop"
       xmlns:tx="http://www.springframework.org/schema/tx"
       xsi:schemaLocation="http://www.springframework.org/schema/beans http://www.springframework.org/schema/beans/spring-beans.xsd http://www.springframework.org/schema/context http://www.springframework.org/schema/context/spring-context.xsd
        http://www.springframework.org/schema/aop http://www.springframework.org/schema/aop/spring-aop.xsd http://www.springframework.org/schema/tx http://www.springframework.org/schema/tx/spring-tx-2.5.xsd">

    <bean id="corePropertyConfigurer"
		class="org.springframework.beans.factory.config.PropertyPlaceholderConfigurer">
		<property name="location">
		    <value>classpath:mail.properties</value>
		</property>
	</bean>
    
    <bean id="mailService" class="com.hundsun.fund.website.mail.core.service.impl.MailServiceImpl">
        <property name="mailSender" ref="mailSender"/>
        <property name="mailSenderSSL" ref="mailSenderSSL" />
        <property name="mailfrom" value="${mail.mailfrom}" />
        <property name="mailto" value="${mail.mailto}" />
        <property name="subject" value="${mail.subject}" />
        <property name="text" value="${mail.text}" />
    </bean>

    <!-- mail服务配置 -->
    <bean id="mailSender" class="org.springframework.mail.javamail.JavaMailSenderImpl">
        <property name="host" value="${mail.mailhost}"/>
        <property name="username" value="${mail.mailname}"/>
        <property name="password" value="${mail.mailpassword}"/>
        <!-- 邮箱身份认证 -->
        <property name="javaMailProperties">
            <props>
                <prop key="mail.smtp.auth">true</prop>
                <prop key="mail.smtp.timeout">25000</prop>
                 <!--如果mail服务器是ssl认证，则去掉这里的注释符号 -->
                <!--<prop key="mail.smtp.socketFactory.port">465</prop>-->
                <!--<prop key="mail.smtp.socketFactory.class">-->
                <!--javax.net.ssl.SSLSocketFactory-->
                <!--</prop>-->
                <!--<prop key="mail.smtp.socketFactory.fallback">-->
                <!--false-->
                <!--</prop>-->
            </props>
        </property>
    </bean>

    <!-- mail服务配置SSL -->
    <bean id="mailSenderSSL" class="org.springframework.mail.javamail.JavaMailSenderImpl">
        <property name="host" value="${mail.mailhost}"/>
        <property name="username" value="${mail.mailname}"/>
        <property name="password" value="${mail.mailpassword}"/>
        <!-- 邮箱身份认证 -->
        <property name="javaMailProperties">
            <props>
                <prop key="mail.smtp.auth">true</prop>
                <prop key="mail.smtp.timeout">25000</prop>
                 <!--如果mail服务器是ssl认证，则去掉这里的注释符号 -->
                <prop key="mail.smtp.socketFactory.port">465</prop>
                <prop key="mail.smtp.socketFactory.class">
                javax.net.ssl.SSLSocketFactory
                </prop>
                <prop key="mail.smtp.socketFactory.fallback">
                false
                </prop>
            </props>
        </property>
    </bean>

    <bean id="mailBuilder" class="com.hundsun.fund.website.mail.mailapi.impl.MailBuilderImpl" init-method="init">
        <property name="mailService" ref="mailService" />
        <property name="SSL" value="${mail.SSL}" />
    </bean>

</beans>